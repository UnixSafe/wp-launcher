package com.example.ui.theme

// The Material You template swatches (Purple/Pink) were removed: this launcher uses the
// Windows Phone accent system instead. The safe accent default lives in ColorUtils.kt
// (WpDefaultAccent) and the user-selectable accents live in SettingsScreen.kt.
